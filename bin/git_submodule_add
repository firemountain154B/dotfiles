#! /bin/bash

if [[ $# -ne 1 ]] ; then
    echo "need one parameter: the repo name"
    exit 1
fi


if [[ $1 == *"/"* ]] ; then
	git submodule add $1
else
	git submodule add "git@github.com:LiuTaowen-Tony/${1}.git"
fi

